@model Milou.Deployer.Web.IisHost.Areas.Agents.AgentsViewModel

<h2>Agents</h2>

@foreach (var agent in Model.Agents)
{
    <div>@agent.Id</div>

    <p>Connected at: @agent.ConnectedAt.ToString("O")</p>
    <p>CurrentDeploymentTaskId: @agent.CurrentDeploymentTaskId</p>
}